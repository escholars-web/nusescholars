# Dependencies
node_modules

# Build output and caches
.next
out
.turbo
.vercel
*.tsbuildinfo
coverage

# Secrets and local environment
.env
.env.local
.env.*.local
*.pem

# Editor and tooling config that is local to one machine
.claude
.idea
.vscode

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Python data pipeline
__pycache__/
*.py[cod]
venv/
.venv/

# Source spreadsheets, the JSON in src/data is what ships
*.xlsx

# macOS
.DS_Store

# Impeccable design skill, installed per developer via `npx impeccable install`.
# The .claude copy is already ignored above. These are the GitHub Copilot copy,
# 150+ vendored files that do not belong in the shared repo.
.github/skills/
.github/agents/
.github/hooks/
